# Checkpoints

Model checkpoints are intentionally not versioned in git because they can be very large.

Suggested ways to share weights:

- GitHub Releases (attach `s1_best.pth` as a release asset)
- Git LFS
- External model hosting (Hugging Face Hub, Google Drive, etc.)
